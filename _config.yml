# Jekyll Configuration - Minimal to avoid interfering with project files
exclude:
  - node_modules
  - frontend/node_modules
  - backend/node_modules
  - frontend
  - backend
  - .git
  - .gitignore

# Build settings
theme: jekyll-theme-primer
